import React from 'react';
import { Link } from 'react-router-dom';
import { format } from 'date-fns';
import Checkbox from '../../input/checkbox';
import CustomButton from '../../button/customButton';
import FormButtons from '../../button/form/formButtons';

const SemesterSection = ({ items, semester, selectedItems, handleDeleteSelected, handleCheckboxChange, handleEdit, children, updateId, itemName, handleSubmit, closeModal }) => {

    return (
        
    );
};

export default SemesterSection;
